<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Base application theme -->
    <style name="Theme.FirstBankOfPig" parent="android:Theme.Material.Light.NoActionBar">
        <!-- Status bar -->
        <item name="android:statusBarColor">@color/pink_500</item>
        <item name="android:navigationBarColor">@color/neutral_50</item>

        <!-- Window background -->
        <item name="android:windowBackground">@color/neutral_50</item>
    </style>

    <!-- AppCompat theme for OssLicensesMenuActivity -->
    <style name="Theme.FirstBankOfPig.AppCompat" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="colorPrimary">@color/pink_500</item>
        <item name="android:statusBarColor">@color/pink_500</item>
    </style>
</resources>
